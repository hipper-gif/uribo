# Uribo - CLAUDE.md

## プロジェクト概要

- **プロジェクト名**: Uribo
- **説明**: 美容部門売上管理ツール（うりぼー）
- **技術スタック**: React + TypeScript + Tailwind + PHP + MySQL
- **リポジトリ**: https://github.com/hipper-gif/uribo

## デプロイ先

- **使う人区分**: 👤 個人（美容部長必須） + 🏢 社内（他部門長/経営層閲覧）※両面持ち
- **公開URL**: https://twinklemark.xsrv.jp/uribo/
- **デプロイ先パス**: `~/twinklemark.xsrv.jp/public_html/uribo/`
- **DB**: `twinklemark_nicolio`（**Nicolio共有DB、beauty_monthly_data 等のテーブル**）
- **⚠️ 共有DB**: DDL変更は Nicolio/Thalia/しんせいくん等に影響。安全フロー必須
- **詳細ルール**: `clio/knowledge/deploy-layout.md`

## 技術スタック

React + TypeScript + Tailwind + PHP + MySQL

## beauty_item_master の運用ルール ★必須

UIに表示される項目は `useItemMaster` (`frontend/src/lib/useBeautyData.ts`) で `is_active=eq.1` でフィルタされている。

### 妻向けダッシュボードに出す項目(`is_active=1`)
- 売上カテゴリ: `sales` / `customers` / `unit_price` の3つだけ
- 経費は 10区分(下記)で大まかにまとめた項目
- 細かい売上内訳(現金日別・カード・電マネ等)は **出さない**

### 経費の10区分(2026-05-23確定・2026-08-26「税金」分離・経営判断軸)

「テコ入れポイントの可視化」と「目標設定時の予測しやすさ」を両立する分類。

| 区分 | 性質 | 主なitem | 予測ロジック | テコ入れ難度 |
|------|------|---------|------------|------------|
| **変動費** | 売上連動 | cogs, supplies, fees | 売上×% | 中 |
| **人件費** | 半固定・人員計画依存 | salary_total, bonus, recruitment, training, transport_total, welfare | 人員計画 | 高 |
| **法定費用** | 強制・削減不可 | legal_welfare, workers_comp | 給与×料率 | 不可 |
| **税金** | 消費税(売上連動) | withholding_tax(預かり税=納付税額), vat_purchase(参考行), net_payable_tax(廃止) | 目標売上・目標経費から自動算出(TargetSetting保存時) | 不可 |
| **契約固定費** | 月額固定・契約縛り | rent, franchise_fee, insurance, shopping_street | 確定値 | 低 |
| **インフラ** | 事業基盤(集客含む) | electricity, gas, water_utility, communication, garbage, **hpb** | 前月±α | 中 |
| **サブスク** | 月額固定・解約容易 | water_supply, microsoft, spotify, amazon_prime | 確定値 | 高 |
| **スポット費用** | 不定期発生 | travel_expense, repair, entertainment, meeting, advertising, outsourcing | 年予算÷12 | 中 |
| **管理費** | 内部按分・固定 | twinkle_fee | 確定値 | 個別 |
| **その他** | 残り | discount, other_expense, 集計派生 | — | — |

### MySQL 認証(twinklemark_nicolio)

| 項目 | 値 |
|------|-----|
| ホスト | localhost (SSH先サーバー内) |
| DB | twinklemark_nicolio |
| ユーザー | twinklemark_app |
| パスワード | twinkle2525 |

SSH経由のDDL実行例:
```bash
ssh -i ~/.ssh/id_xserver_panel -p 10022 twinklemark@sv16114.xserver.jp \
  "mysql -u twinklemark_app -ptwinkle2525 twinklemark_nicolio < /tmp/migration.sql"
```

### ★ENUMカラム事故の教訓 (2026-05-23)

`beauty_item_master.item_category` が **ENUM('売上','仕入','人件費','法定福利','固定費','税金','その他')** で固定されていた。
新カテゴリ値('変動費'/'契約固定費'/'インフラ'/'サブスク'/'スポット費用'/'管理費'/'法定費用')をAPI経由でUPDATEした時、ENUM対象外のためMySQLが **silent に空文字列で保存**(警告のみ)し、ダッシュボードで項目が消える事故が発生。

対策: `sql/003_item_category_to_varchar.sql` で VARCHAR(20) に型変更済み。
**新カテゴリ追加・変更は他テーブルでも ENUM 制約をまず確認すること**(`SHOW CREATE TABLE ...`)。

### 預かり税(消費税)の運用方針 ★

会計上は預り金(BS負債)だが、うりぼーは **キャッシュ視点で月次経費として継続計上**する。

- `withholding_tax`(預かり税): is_calculated=0(通常項目) / **税金カテゴリ**(2026-08-26 に法定費用から分離。ADR-0004) / **自動計算** `floor((sales−discount)/11) − floor(課税仕入合計/11)` = **実質納付税額**
  - 値は DataEntry の保存時に自動でDBにセットされる
  - 「課税仕入合計」 = NON_TAXABLE_ITEM_CODES に含まれない経費の税込合計
- `vat_purchase`(仕入消費税): is_calculated=1 / 自動計算 `floor(課税仕入合計/11)` / 税金カテゴリの **参考行(展開時のみ表示)**・集計加算しない
- `net_payable_tax`(納付税額): **is_active=0**(廃止) / withholding_tax が既に納付税額のため冗長
- **集計**: `withholding_tax`(=納付税額) は税金カテゴリの月次合計に算入され、支出合計・純利益から減算される
- **目標側**: TargetSetting の保存時に目標値から同じ式で自動算出して書く(ADR-0004 D25)。保存を経ていない年度は `npm run tkc-import -- --recalc-only --year YYYY --month M --data-type 目標 --apply` で穴埋め(D26)。目標に消費税が無いと税金区分の達成率が成立しない
- **税込→税抜の変換装置**: 売上税込−経費税込−(売上消費税−仕入消費税)＝売上税抜−経費税抜 なので、うりぼーの純利益はTKCの税抜営業利益と(減価償却・賞与引当・Twinkle按分・端数を除き)一致する。二重計上ではない(2026-08-26 検算済)
- これにより「税込で経費計上 + 仕入消費税は控除されない」だった旧仕様の二重計上気味問題が解消、実際のキャッシュアウトに一致する
- **理由**: 妻ダッシュボードで「手元に残るリアルな金」を見るため。預かり税分は「いずれ国に納める拘束された金」として最初から差し引いて表示
- **TKC比較時の注意**: TKC側は預かり税を経費にしない(BS処理)ため、TKC比較ページでは差分が出る。これは仕様。

### tax-exclusive 入力する item

`TAX_EXCLUSIVE_INPUT_CODES = { 'cogs', 'supplies' }`
UIでは税抜入力、DBでは×1.10して税込保存。受領レシートが税抜のため。

### water_supply / welfare の使い分け(重複回避)

- **`water_supply`**「水(ウォーターサーバー)」**= 富士山の天然水(月額)** = TKC 6226福利厚生費の美容部門分。サブスク区分。
- **`welfare`**「福利厚生費」は water_supply 以外の福利厚生費(健康診断・社員旅行・贈答品・飲食等)。**2026-08-26 に is_active=1 へ再有効化**(2026/05 守口 スターバックス 4,030 が発生したため。ADR-0005 D33)。
- TKC 6226 → 取引先が富士山の銘水なら `water_supply`、それ以外は `welfare`(`TKC_SUBCLASSIFIERS['6226']`)。`TKC_RULES['6226'].uribo_codes` に両方を入れておくこと(片方だけだと0初期化されず旧値が残る→2026/04 両店で welfare 3,300 が water_supply と二重になっていた。削除済)。
- 経緯: 2026-05-23 に TKC 6226 対応で welfare を新規追加したが、美容部門の6226は富士山の天然水のみで water_supply と完全に同義だったため welfare を非アクティブ化。

### ちょぼまる連携用の細粒度 item(`is_active=0`)
- 売上の細かい内訳(`cash_sales_d01_05`、`card_sales_d01_05` 等)はちょぼまるが TKC仕訳のために使う
- うりぼー UI では非表示(`is_active=0`)、DBにはデータ保持、ちょぼまるからは取得可能
- ちょぼまる側のクエリは `item_code=like.*sales*` で `is_active` フィルタを使っていないので問題なく読める

### 新規 item を追加する時のルール
- **デフォルトで `is_active=0` を指定する**
- 「妻にも見せたい」項目を新設するときだけ `is_active=1` にする
- 過去事故: 2026-05-22 に 18件の5日単位売上 item が `is_active=1` で投入され、ダッシュボードに「—」行がずらっと表示された

### ちょぼまる側ルール
`C:/Users/nikon/projects/chobomaru/CLAUDE.md` に同じルールを記載済み(対の関係)。

---

## TKC連携のイレギュラー運用ルール ★必須

TKC(税理士確定値)とうりぼーの数値は、以下の運用上のズレが**意図的にある**。
インポート機能や TKC 比較で「差分=エラー」と判定しないこと。

### 1. Twinkle代 (`twinkle_fee`)
- 実態: **爽夏さんへの給料**。介護部門も兼任しているため**部門間で按分**
- TKC: 6117外注費に**美容部門全額(例: 4月 171,000円)** が計上される
- うりぼー算式: **(TKC 6117のTwinkle代相当 − 40,000円(介護按分)) ÷ 2 = 各店舗 twinkle_fee**
  - 例: 4月 (171,000 − 40,000) ÷ 2 = 65,500円/店舗
- `MGMT_FEE_CODE = 'twinkle_fee'` で支出合計から除外される独立科目
- 取引先名「テインクル/ティンクル/twinkle/杉原/爽夏」で識別
- ★**店別計上への移行(2026-08-26 杉原氏「うりぼーの折半が正」)**: TKCの部門損益は6117を寝屋川011に美容全額一本で計上するため店別が歪む(寝屋川 約−95k/守口 約+59k)。税理士/ちょぼまる(Clio)の仕訳入力を **011に65,000「Twinkle代 寝屋川」・012に65,000「Twinkle代 守口」・003に40,000「Twinkle代 介護」** へ寄せる。取込は両店にTwinkle代が立っていれば店別の額をそのまま、片店一本なら従来の `(合計−TWINKLE_KAIGO_DEDUCT 40,000)÷2` で按分(ADR-0004 D28)。**守口の65,000にタグ/本人名が無いと和田判定に倒れる**ので摘要タグは必須

### 2. 和田の委託費
- TKC: 6117 外注費に計上
- うりぼー: **守口店の `salary_total`(人件費)に加算する給料扱い**
- 理由: 給料計算はうりぼーで完結させたいため
- ★**和田委託費もTKC上は取引先名が"Twinkle"名義になることがある**(2026/05 守口の「Twinkle 委託販売手数料 65,000」が実は和田委託で、Twinkle代と誤判定された)。
  対策: 仕訳の摘要/取引先に**「和田」を明記**してもらい、`classifyOutsourcing` は**和田判定をTwinkle判定より先に**実行する(和田と書いてあればTwinkle名義でも和田委託)。Twinkle代の基準額に和田分を足さないこと。
- ★**「和田」明記は徹底されていない前提で扱う**(2026/06 も明記なしで再発)。`classifyOutsourcing` の自動判定(優先順・2026-08-26 改定 ADR-0004 D27): ①「和田」明記→和田 ②「今道/コンドウ」→今道(業務委託) ③**摘要タグ「Twinkle代」または本人名(杉原/爽夏/サヤカ)→Twinkle代** ④**金額が定額65,000円(`WADA_FIXED_FEE`)→和田**(タグも本人名も無い行の最後の砦。額が変わったら定数を更新) ⑤守口(012)のTwinkle名義で本人名なし→和田／寝屋川→Twinkle代。**Clio/ちょぼまるが仕訳を作る時の摘要規約**＝「和田 委託販売手数料」「今道 …」「Twinkle代 寝屋川/守口/介護」(chobomaru `mapping_rules.json` と対)。自動判定が外れた場合は、TKCインポートのプレビューで**6117明細ごとに「Twinkle代/和田委託/その他外注」をプルダウンで手動修正**できる(2026-07-16 追加)。
- ★**インポート挙動(2026-06-18変更)**: 和田分(6117)は無視せず、計上店舗(守口)の **`salary_total` に加算**する。execute側で同一(store,item)のdraftを合算するので、6212由来の給与と自動で合算される。
- ★**和田は「PX2の時給パート給与」と「委託販売手数料65,000」の二本立て**(2026-08-14 確認)。6212(未払給与の一本計上)に含まれるのは**給与だけ**なので、6117の65,000を足しても二重にはならない。裏取り＝2026/07は 給与63,050(PX2支給実績=うりぼーpayroll emp78 gross_total)と 委託65,000 が 7/10 に**別々の振込**で実在し、守口の6212 925,950 は payrollのgross_total合計941,850−交通費15,900 と完全一致（65,000の余地が無い）。
- ★**委託65,000は毎月あるとは限らない**(facts F15)。和田を**扶養内に収めるための調整弁**で、給与と合わせた総額が65,000に届かない月・少し超える月は**その月は委託費なしで行く**（実例＝2026/04は委託ゼロ）。**「毎月あるはず」の欠落検知を足さないこと**。

### 2.5 加盟金/ロイヤルティ(6118)の折半按分
- 実契約は**各店等額(20,000円/月)**。TKC上も本来は部門011/012に20,000ずつ計上されるのが正
- ★片部門に全額一本で計上されるのは**仕訳ミス**(2026/06: 011寝屋川に40,000一本。杉原氏確認済み)。J4監査が「仕訳そのものの疑い」として警告し、**税理士へ部門修正を依頼する**
- インポートは防御として全6118エントリを合算して**2店舗で折半**し各店 `franchise_fee` へ(仕訳が正しくても誤っていても結果は各店20,000で同じ)
- ★片店に全額のまま取り込み+もう片店を手入力すると、合計が仕訳帳と乖離する(40,000→60,000)。折半なら合計一致
- 片店だけの臨時ロイヤルティが発生した場合はプレビューの反映値を手修正
- R18(親子合算ズレ監査)は6117と同様に6118も除外(クロス店舗按分のため)

### 2.7 今道寿子の業務委託費（★2026-08-07 確定）
- 今道寿子(emp_id=84・守口店・**TKCコード無し=給与明細/支給控除一覧表には載らない**)は**業務委託**。報酬 = **本人売上×50%**(変動)
- **支払元は Smiley。TKC 6117 外注費に計上される**。支払いの受け渡しがリアライズ経由なだけで、費用はSmiley負担
- うりぼー: **和田と同じく守口店の `salary_total`(人件費)に加算**する(給料計算をうりぼーで完結させる方針)
- ★「PX2に載せない」＝**給与システム(PX2)に載らない**という意味であって、会計(TKC 6117)には載る。ここを取り違えると外注費へ誤計上する(2026-08-07 に実際に誤読が発生)
- 金額は変動するため`WADA_FIXED_FEE`のような金額判定は使えない。**名義(今道/イマミチ)のみ**が判定材料 → 摘要に「今道」を残してもらうこと

### 2.8 貸方の相殺・部門振替・引当の除外(ADR-0005・2026-08-26)
- **費用科目の貸方は同じ科目からネット**する(`aggregateBeauty`)。対象例: 6312 の従業員負担**雇用保険**(月末給与仕訳で `CR 6312/雇用保険料`・預り金ではない)、5211 の返品返金、6225 の立替金振替。これをしないと法定福利費が毎月 +2.7〜5.2k/店 過大(総支給に含まれる従業員負担分の二重費用化)
- **部門振替(期中修正)は伝票単位で除外**(`correctionVoucherKeys`): 伝票内の科目が全部損益科目のものだけ。相手科目に売掛金等のBS科目があれば本物の費用(例 2026/05/01 PayPay手数料「期中修正」6,526×2)。除外した伝票は監査J5に出る。**修正対象の月のうりぼー値が正しいか人が確認する**
- **引当計上(`CR 2126`・「引当金計上」)は集計しない**(6213 賞与引当 12,500/店/月 = 非現金)。実支給の賞与はTKC経由で入らないので `bonus` は給与明細から手入力
- ネット後に負になった科目は監査J6が警告(返金だけが翌月にずれた等)

### 3. TKC 6117 外注費の扱い
- 中身: **Twinkle代(介護按分前の美容部門全額) + 業務委託費(和田・今道) + その他真の外注**
- **そのまま `outsourcing` にインポートしてはダメ**(二重計上になる)
- インポート時は 6117 を取引先名で分類(`classifyOutsourcing` → `'twinkle' | 'itaku' | 'other'`):
  - Twinkle代相当 → 別ロジックで `twinkle_fee` に按分(項目1参照)
  - **業務委託(`itaku`)= 和田・今道 → その店舗(守口)の `salary_total` に加算**(6212従業員給与に両名とも含まれないため)
  - その他真の外注 → `outsourcing`

### 4. 家賃まわりの構成(2026-08-14 銀行明細で全額確定)

毎月の固定費振込は3本。TKC 6215(地代家賃)には**このうち3本すべてが「固定費」の一言で計上される**（取引先名も空欄）ので、金額でしか区別できない。

| 振込先 | 金額 | 中身 | うりぼーの行き先 |
|---|---|---|---|
| ヒガシウチ マサミ | 124,000 | 寝屋川店の家賃121,000 + 水道3,000 | `rent` 121,000（水道はTKC側で6219に分離済→`water_utility`） |
| マサキ カズミツ | 100,000 | 守口店の家賃 | 守口 `rent` |
| オオトシシヨウテンガイシンコ | 11,800 | 大利商店街振興組合費(寝屋川) | `shopping_street`（**6215に紛れているので `SHOPPING_STREET_FEE` の金額判定で分離**） |

- ★**商店街費(11,800)は6215に混ざる。** 分離しないと全額が `rent` に寄り `shopping_street` が毎月0になる（2026/06・07が実際にそうなった）。逆に `shopping_street` を手入力していた月（2026/04・05）は rent と**二重計上**になっていた
- ★**2026/05のTKC仕訳は部門が逆**: 011に100,000・012に124,000（正しくは011に121,000+3,000分離・012に100,000）。**税理士へ部門修正の依頼が必要**。うりぼーは `--skip 6215,6219` で取込から外し、正しい値を保持している

### 5. 過去 `rent` データの実態乖離 (2025fy以前)
- 過去のうりぼー `rent`(月426K等)は「**家賃以外の固定費を混ぜ込んだ総額**」
- 実態の家賃は TKC 6215 の値が真。寝屋川店分しか TKC にない可能性大、守口店分は別経路の疑い
- **過去 rent をそのまま比較・遡及修正してはいけない**。2026年4月以降を「正しい仕分け」で運用

### 6. cogs と supplies の分離
- 過去(2025fy以前): `cogs` に材料仕入と備品消耗品が混在
- 2026年4月以降: 寝屋川店から分離開始(`cogs`=材料、`supplies`=ハサミ/コーム/タオル等)
- TKC マッピング: 5211 材料仕入高 → `cogs` / 6225 備品消耗品費 → `supplies`

### 7. transport_total (通勤手当) の運用
- `transport_total` は `is_calculated=1 / is_active=0` だが、**実際は値が入力されている**(2026年4月時点)
- 給料計算ルート(Payroll経由)で集計される独自運用
- 新規追加した `commute_allowance` は **不要** → is_active=0 に戻す
- TKC 6111 通勤交通費 → `transport_total` にマッピング(commute_allowance は使わない)

### 8. 守口店 2026年4月の入力欠落
- rent / cogs / supplies / 固定費類が未入力(2026-05-23 確認時点)
- TKCインポートで補完予定

### インポート機能の設計含意
- 売上(4111): `(TKC × 1.10) + 既存 discount` で復元、discount は触らない
- 6117 外注費: 全額自動取込せず、プレビューで手動振り分け
- 6215 家賃: 寝屋川店分は実態値で上書きOK、守口店分は別途確認。税理士の期中修正(部門振替)が入った月は伝票ごと除外される(2.8)
- 1対多TKC科目(6113/6116/6218/6219/6312): プレビューで各 uribo item へ分配入力
- 3月決算月: 期末調整があるためプレビュー必須・自動取込除外推奨
- うりぼー独自(discount/twinkle_fee/withholding_tax/派生計算): インポート対象外

### 取り込みの実行方法（画面 / CLI の2経路・2026-08-14 追加）

**画面**: うりぼー → TKCインポート（`pages/TkcImport.tsx`）。プレビューで6117の明細区分を手修正できるのはこちら。

**CLI**（Clioに仕訳帳CSVを渡して反映してもらうとき。ADR-0002）:

```bash
cd frontend
npm run tkc-import -- --csv "C:/Users/.../仕訳帳.csv"            # dry-run(既定・書き込まない)
npm run tkc-import -- --csv "..." --apply                        # 反映
npm run tkc-import -- --recalc-only --year 2026 --month 6        # 預かり税だけ検算
```

- 対象月・会計年度はCSVの日付から自動判定（複数月混在なら `--month` 必須）
- 変換規則も異常検知も `src/lib/tkcImport.ts` / `tkcAudit.ts` **をそのまま呼ぶ**（CLIはI/Oだけ）。この節のルールを変えたらlibを直せば画面とCLIの両方に効く
- 監査に「要対応」があると `--force` なしでは書き込まない。UPSERTなので同じCSVを何度流しても冪等
- **★取り込みは預かり税(`withholding_tax`)を自動で再計算して書き戻す**。TKCで課税仕入(経費)が変わると納付税額も変わるため。画面から取り込んだ場合はこの再計算が走らないので、**画面運用のときは取り込み後にDataEntryで各店を保存し直すこと**（忘れると預かり税が控除前のまま残り純利益が過小に見える。2026年度4月・6月が実際にその状態だった＝2026-08-14に是正済み）
- DBアクセスはSSH経由mysql（`~/.ssh/id_xserver_panel` が要る）

### ★正しさ検証(TKC残差突合)との同期 — 必須

`frontend/src/lib/tkcRecon.ts` の `RECON_RULES`(意図的差分の宣言表)は、**この「TKC連携イレギュラー運用」節と1対1**で対応する。TKC比較ページの「残差突合」は `残差 = うりぼー − (TKC税込換算 + 意図的差分)` で会計的な正しさを検証する仕組み。
- **このイレギュラー運用(Twinkle按分・和田→守口給与・家賃水道分離・cogs/supplies分離 等)を変更したら、必ず `RECON_RULES` も同じ値に更新する。** 同期を怠ると、正しいデータを誤判定 or 真の誤りを見逃す(突合が無意味化)。
- TKC残高推移表の税区分は税抜前提(`TKC_BALANCE_IS_TAX_EXCLUSIVE=true`)。税込出力なら false に。
- 人件費・外注(6212+6117)は税区分混在・按分のため自動判定せず手動確認(mode:'manual')。

---

## Clio連携ルール（必須・全プロジェクト共通）

このリポジトリは **Clio（パーソナル秘書AI）** のタスク管理対象です。
サブセッション（このリポジトリで開発作業をするClaude Codeセッション）は、以下のルールに従ってください。

### SPEC尊重ルール（磨き込み沼防止・必須）

- リポジトリ直下の **SPEC.md が スコープの正本**。実装は SPEC.md の中核(Must)と受入基準に従う
- **SPEC.md に記載のない改善（UX磨き・リファクタ・機能追加の思いつき）は実装しない。** SPEC.md の `対象外(Won't)` の W-later に1行記録して報告するだけにする
- 実装順: 中核(Must)→受入基準 全緑=テスト中リリース → その後に準中核(Should)（公開中昇格の条件）。任意(Could)は指示があるときだけ
- Should欄はSPEC確定時に凍結済み。実装中の思いつきをShouldに追加しない（W-laterへ記録）
- SPEC.md が無い既存プロジェクトでは、Clio側の「リリース判定チェック」結果（ROADMAP.md の DoD）を正とする
- 型の定義: `clio/knowledge/spec-method.md`

### Nicolio API 情報

| 項目 | 値 |
|------|-----|
| API URL | `https://twinklemark.xsrv.jp/nicolio-api/api.php` |
| 認証 | `Authorization: Bearer nicolio_secret_2525xsrv` |
| タスク更新 | `PATCH /tasks?id=eq.{UUID}` |

### タスクステータス即時更新（必須）

**作業が進んだら、その場でタスクのnext_actionとstatusを更新する。セッション終了まで待たない。**

以下のいずれかに該当したら即更新:
- 成果物（ファイル・コード・設計書等）を完成した
- git commit & push した
- ブロッカーが解消された（前提機能の完成等）
- フェーズが進んだ（要件定義→実装等）

**next_actionの書き方**: 完了した内容（過去形）ではなく、**次にやるべきこと**を書く。

```bash
# 更新例
python -c "
import urllib.request, json
url = 'https://twinklemark.xsrv.jp/nicolio-api/api.php/tasks?id=eq.{TASK_UUID}'
data = json.dumps({'next_action': '次にやること', 'status': '進行中'}).encode()
req = urllib.request.Request(url, data=data, headers={
    'Authorization': 'Bearer nicolio_secret_2525xsrv',
    'Content-Type': 'application/json'
}, method='PATCH')
urllib.request.urlopen(req)
"
```

### セッション完了時プロトコル（必須）

セッション終了前に**必ず**以下を実行:

1. **git commit & push** — 未コミット変更を残さない
2. **タスク更新** — next_actionが「次のステップ」を指しているか確認。完了したアクションが残っていたら書き換える
3. **git status** — 未コミットがないことを最終確認

### Git運用ルール

- デプロイしたら**必ず** git commit & push（デプロイ済みコードがGitHub未反映の状態を残さない）
- 作業の区切り（機能追加・バグ修正・設定変更等）ごとにこまめに commit & push
- コミットメッセージは日本語で簡潔に
